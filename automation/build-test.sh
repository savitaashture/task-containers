#!/usr/bin/env
# The shell script is run as a "setup" process before running `make e2e`.
# This mean we should have here any requirement setup correctly.
# This can mean installing required tools that are not in the image used, etc.
